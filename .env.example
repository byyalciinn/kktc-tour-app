# ===========================================
# Tour App Environment Variables
# ===========================================
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun
# cp .env.example .env

# -------------------------------------------
# Supabase Configuration
# -------------------------------------------
# Bu değerleri Supabase Dashboard'dan alın: https://app.supabase.com
# Project Settings > API bölümünden

EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# -------------------------------------------
# App Configuration (Optional)
# -------------------------------------------
# EXPO_PUBLIC_APP_ENV=development
# EXPO_PUBLIC_API_TIMEOUT=30000

# -------------------------------------------
# Push Notifications (Required for push)
# -------------------------------------------
# Get this from EAS: npx eas project:info
EXPO_PUBLIC_PROJECT_ID=your-eas-project-id-here

# -------------------------------------------
# Email Service (Resend API for 2FA)
# -------------------------------------------
# Get your API key from https://resend.com/api-keys
# These are set in Supabase Edge Functions secrets, NOT in .env
#
# Deploy the Edge Function:
#   supabase functions deploy send-verification-email --no-verify-jwt
#
# Set secrets:
#   supabase secrets set RESEND_API_KEY=re_xxxxx
#   supabase secrets set FROM_EMAIL=noreply@yourdomain.com
#
# Note: FROM_EMAIL must be a verified domain in Resend

# -------------------------------------------
# AI Vision API Keys (SECURITY UPDATE)
# -------------------------------------------
# API keys are now managed via Supabase Edge Functions for security.
# DO NOT expose API keys in client-side code!
#
# Set these in Supabase Dashboard > Edge Functions > Secrets:
# - OPENAI_API_KEY (from https://platform.openai.com/api-keys)
# - ANTHROPIC_API_KEY (from https://console.anthropic.com)
#
# Deploy the Edge Function:
# supabase functions deploy analyze-image
# supabase secrets set OPENAI_API_KEY=sk-your-key
# supabase secrets set ANTHROPIC_API_KEY=sk-ant-your-key

# -------------------------------------------
# Adapty SDK (In-App Purchases)
# -------------------------------------------
# Get your Public SDK Key from Adapty Dashboard:
# https://app.adapty.io > Settings > Public SDK key
EXPO_PUBLIC_ADAPTY_PUBLIC_KEY=public_live_xxxxxxxx

# -------------------------------------------
# Feature Flags (Optional)
# -------------------------------------------
# EXPO_PUBLIC_ENABLE_ANALYTICS=false
# EXPO_PUBLIC_ENABLE_CRASH_REPORTING=false

# -------------------------------------------
# IMPORTANT SECURITY NOTES
# -------------------------------------------
# 1. NEVER commit .env file to version control
# 2. NEVER expose service_role key in client-side code
# 3. Always use Row Level Security (RLS) policies
# 4. Rotate keys if they are accidentally exposed
